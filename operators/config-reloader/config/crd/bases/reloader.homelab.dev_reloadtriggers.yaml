# Generated by controller-gen from api/v1alpha1/reloadtrigger_types.go —
# never hand-edit this file; running
#   controller-gen crd paths="./api/v1alpha1" output:crd:artifacts:config=config/crd/bases
# regenerates it and would silently discard any manual change.
#
# This is the object that actually teaches the Kubernetes API server what
# "kind: ReloadTrigger" means: without applying this CRD first, creating a
# ReloadTrigger object anywhere in the cluster fails with
# "no matches for kind ReloadTrigger" — the same "CRD before CR" ordering
# risk called out in the design spec and in apps/applications/config-reloader.yaml.
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: reloadtriggers.reloader.homelab.dev
spec:
  group: reloader.homelab.dev
  names:
    kind: ReloadTrigger
    listKind: ReloadTriggerList
    plural: reloadtriggers
    singular: reloadtrigger
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.targetDeployment
      name: Target
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].reason
      name: Reason
      type: string
    - jsonPath: .status.lastReloadTime
      name: Last Reload
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ReloadTrigger opts a Deployment into automatic rolling restarts whenever
          a ConfigMap/Secret it references changes content.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ReloadTriggerSpec defines which Deployment to restart and which resources
              to watch for content changes that should trigger that restart.
            properties:
              targetDeployment:
                description: |-
                  TargetDeployment is the name of the Deployment, in this object's own
                  namespace, to restart when a watched resource's content changes.
                minLength: 1
                type: string
              watch:
                description: |-
                  Watch lists the ConfigMaps/Secrets, in this object's own namespace,
                  whose content is combined into a single hash. Any change to any of
                  them triggers a restart.
                items:
                  description: |-
                    WatchedResource references one ConfigMap or Secret, in the ReloadTrigger's
                    own namespace, whose content should be watched for changes.
                  properties:
                    kind:
                      description: WatchedResourceKind identifies the kind of resource
                        a ReloadTrigger watches.
                      enum:
                      - ConfigMap
                      - Secret
                      type: string
                    name:
                      minLength: 1
                      type: string
                  required:
                  - kind
                  - name
                  type: object
                minItems: 1
                type: array
            required:
            - targetDeployment
            - watch
            type: object
          status:
            description: |-
              ReloadTriggerStatus reports the last observed content hash and the result
              of reconciling it.
            properties:
              conditions:
                description: |-
                  Conditions represents the latest observations, keyed by Type. The
                  controller maintains a single condition of type "Ready".
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastReloadTime:
                description: |-
                  LastReloadTime is when the target Deployment was last restarted by
                  this ReloadTrigger. Unset if it has never restarted anything.
                format: date-time
                type: string
              observedHash:
                description: |-
                  ObservedHash is the sha256 hex digest of the combined content of all
                  watched resources, as of the last successful reconcile.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
