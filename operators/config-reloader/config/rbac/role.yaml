# Generated by controller-gen straight from the +kubebuilder:rbac markers
# above ReloadTriggerReconciler.Reconcile in
# controllers/reloadtrigger_controller.go — regenerate via:
#   controller-gen rbac:roleName=manager-role paths="./controllers" output:rbac:artifacts:config=config/rbac
# Never hand-edit: the markers are the actual source of truth, this is
# their compiled output. A ClusterRole (not a namespaced Role) because
# ReloadTrigger objects — and the ConfigMaps/Secrets/Deployments they
# reference — can live in any namespace; see
# config/rbac/role_binding.yaml for how this gets bound to the
# controller's ServiceAccount.
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: manager-role
rules:
- apiGroups:
  - ""
  resources:
  - configmaps
  - secrets
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - ""
  resources:
  - events
  verbs:
  - create
  - patch
- apiGroups:
  - apps
  resources:
  - deployments
  verbs:
  - get
  - list
  - patch
  - watch
- apiGroups:
  - reloader.homelab.dev
  resources:
  - reloadtriggers
  verbs:
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - reloader.homelab.dev
  resources:
  - reloadtriggers/status
  verbs:
  - get
  - patch
  - update
